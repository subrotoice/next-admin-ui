import { ReactNode } from "react";

const FolderNameLayout = ({ children }: { children: ReactNode }) => {
  return (
    <div>
      FolderNameLayout
      {children}
    </div>
  );
};

export default FolderNameLayout;
